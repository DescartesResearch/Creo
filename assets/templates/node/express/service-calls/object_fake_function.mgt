export function {{name}}() {
    const obj = {};

    {{#each props}}
    {{#if required}}
    obj['{{name}}'] = {{fake_func.name}}({{fake_func.args}});
    {{else}}
    if (Math.random() >= {{exclude_probability}}) {
         obj['{{name}}'] = {{fake_func.name}}({{fake_func.args}});
    }
    {{/if}}
    {{/each}}

    return obj
}