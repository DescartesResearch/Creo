[build-system]
requires = ["setuptools>=63.0"]
build-backend = "setuptools.build_meta"

[project]
name = "seed_user"
description = "Seed the user database collection."
version = "1.0.0"
requires-python = ">=3.9"
dependencies = ["pydantic>=2.5.2,<2.6.0", "motor>=3.2.2,<3.3.2"]
